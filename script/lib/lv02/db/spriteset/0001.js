'use strict'; {

let id = parseInt(wgt.getFileName());

let mod = wgt.db.spriteset[id] = {
    name: 'DemonFighter',

    frameWidth: 111,
    frameHeight: 62,

    originX: 39,
    originY: 24,

    solid: true,
};

}
