'use strict'; {

const id = parseInt(wgt.getFileName());

const mod = wgt.db.spriteset[id] = {
    name: 'DemonFighter',

    frameWidth: 111,
    frameHeight: 62,

    originX: 39,
    originY: 24,

    solid: true,
};

}
